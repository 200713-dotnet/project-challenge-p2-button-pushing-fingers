jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2.3.1
      - run: dotnet build

  test:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2.3.1
      - run: dotnet test -p:CollectCoverage=true -p:CoverletOutputFormat=opencover
  
  # analyze:
  #   needs: [build, test]
  #   runs-on: ubuntu-18.04
  #   steps:
  #     - uses: actions/checkout@v2.3.1
  #     - run: git fetch --unshallow
  #     - run: dotnet tool install --global dotnet-sonarscanner
  #     - run: |
  #         dotnet sonarscanner begin -o:'200713-dotnet' k:'200713-dotnet_project-challenge-p2-button-pushing-fingers' -d:sonar.login='<insert some token here>' -d:sonar.host.url='https://sonarcloud.io/'
  #         dotnet build
  #         dotnet test -p:CollectCoverage=true -p:CoverletOutputFormat=opencover --logger trx
  #         dotnet sonarscanner end -d:sonar.login='<insert some token here>'
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
  
  #package

name: cibuild
on: push